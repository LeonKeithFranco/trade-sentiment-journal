[project]
name = "frontend"
version = "0.1.0"
description = "Frontend workspace for the project."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "plotly>=6.8.0",
    "pydantic-settings>=2.14.1",
    "streamlit>=1.58.0",
]
