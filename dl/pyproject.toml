[project]
name = "dl"
version = "0.1.0"
description = "Workspace for building the deep learning model."
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.8.5",
    "marimo>=0.23.8",
    "torch>=2.12.0",
]
